---
import type { CollectionEntry } from 'astro:content';
import Text from '../text/text.astro';
import { Icon } from 'astro-icon/components';
import IconButton from '@components/core/image/icon-button.astro';
interface Props {
	project: CollectionEntry<'project'>;
}

const { project } = {
	project: Astro.props.project
};

const contributions = project.data.projectInfo.contributions;
---

<ul class="flex list-inside list-disc flex-col text-ellipsis md:list-outside">
	{
		contributions.map((contribution) => (
			<li>
				<span class="text-paragraph">{contribution}</span>
			</li>
		))
	}
</ul>
