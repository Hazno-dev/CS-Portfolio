---
import { Media, type MediaParams } from '@components/media/data/Media.ts';

interface Props extends MediaParams {
	Source: Media | null;
}

const { Source, ...attrs } = {
	...Astro.props
};

if (!Source) {
	return;
}

const data = await Source.GetHtml(attrs);
---

<Fragment set:html={data} />
